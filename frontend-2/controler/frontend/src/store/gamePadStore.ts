import { create } from "zustand";
import { persist } from "zustand/middleware";
import Cookies from "js-cookie";

const useGamePadStore = create()(
    (set, get) => ({

    })
)

export default useGamePadStore;